package com.example.jibi;

import android.os.Bundle;
import android.widget.TextView;

import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;

public class TraitementUserdata extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_traitement_userdata);
        /*
        // Récupérer le TextView où afficher la valeur
        TextView resultTextView1 = findViewById(R.id.TextResult1);
        TextView resultTextView2 = findViewById(R.id.TextResult2);
        TextView resultTextView3 = findViewById(R.id.TextResult3);
        TextView resultTextView4 = findViewById(R.id.TextResult4);

        // Récupérer la valeur envoyée par l'Intent
        String usernameValue = getIntent().getStringExtra("username_value_edit");
        String passwordValue=getIntent().getStringExtra("password_value_edit");
        String emailValue = getIntent().getStringExtra("email_value_edit");
        String datebirthValue=getIntent().getStringExtra("datebirth_value_edit");

        // Afficher la valeur dans le TextView
        resultTextView1.setText("Mr7ba bik si(lala) " + usernameValue);
        resultTextView2.setText("mot de passe dyalk howa " + passwordValue+" ,kijatk securité");
        resultTextView3.setText("email  " + emailValue);
        resultTextView4.setText("zayd f " + datebirthValue);*/


    }
}